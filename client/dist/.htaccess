# Enable CORS headers for API requests
<IfModule mod_headers.c>
  # Allow requests from frontend origins
  Header set Access-Control-Allow-Origin "https://wolfsupplies.co.uk"
  Header set Access-Control-Allow-Origin "https://www.wolfsupplies.co.uk"
  Header set Access-Control-Allow-Methods "GET, POST, PUT, DELETE, PATCH, OPTIONS"
  Header set Access-Control-Allow-Headers "Content-Type, Authorization, X-Guest-ID"
  Header set Access-Control-Expose-Headers "X-Guest-ID, Content-Type, Set-Cookie"
  Header set Access-Control-Allow-Credentials "true"
  Header set Access-Control-Max-Age "3600"
</IfModule>

<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  
  # Handle OPTIONS requests for CORS preflight
  RewriteCond %{REQUEST_METHOD} OPTIONS
  RewriteRule ^(.*)$ - [L]
  
  # Don't rewrite if it's a real file or folder
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  
  # Rewrite all other requests to index.html
  RewriteRule ^(.*)$ index.html [L]
</IfModule>

# Enable compression
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript application/x-javascript
</IfModule>

# Browser caching
<IfModule mod_expires.c>
  ExpiresActive On
  
  # Images
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/gif "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/webp "access plus 1 year"
  ExpiresByType image/svg+xml "access plus 1 year"
  
  # CSS & JavaScript
  ExpiresByType text/css "access plus 1 month"
  ExpiresByType application/javascript "access plus 1 month"
  ExpiresByType text/javascript "access plus 1 month"
  
  # Fonts
  ExpiresByType font/ttf "access plus 1 year"
  ExpiresByType font/otf "access plus 1 year"
  ExpiresByType font/woff "access plus 1 year"
  ExpiresByType font/woff2 "access plus 1 year"
  ExpiresByType application/font-woff "access plus 1 year"
  
  # HTML - Don't cache
  ExpiresByType text/html "access plus 0 seconds"
  
  # Default
  ExpiresDefault "access plus 2 days"
</IfModule>

# Add expires headers
<IfModule mod_headers.c>
  <FilesMatch "\.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|otf|eot)$">
    Header set Cache-Control "public, max-age=31536000, immutable"
  </FilesMatch>
  
  <FilesMatch "\.html$">
    Header set Cache-Control "public, max-age=0, must-revalidate"
  </FilesMatch>
</IfModule>

# Enable GZIP compression for JavaScript and CSS served by the app
<FilesMatch "\.js$|\.css$">
  <IfModule mod_gzip.c>
    mod_gzip_on Yes
    mod_gzip_minimum_file_length 1000
    mod_gzip_minimum_file_length 1000
    mod_gzip_types text/plain text/html text/xml text/css text/javascript application/x-javascript
  </IfModule>
</FilesMatch>

